{
  "name": "packet-run-server",
  "version": "1.0.0",
  "description": "All software components for the Packet Run installation that run on a server",
  "main": "",
  "scripts": {
    "start": "turbo run start",
    "build": "turbo build",
    "dev": "concurrently \"redis-server\" \"turbo run dev\"",
    "lint": "turbo run lint",
    "postinstall": "patch-package"
  },
  "author": "Bureau Moeilijke Dingen",
  "license": "MIT",
  "workspaces": [
    "./packages/*"
  ],
  "dependencies": {
    "patch-package": "^8.0.0",
    "turbo": "^2.5.3"
  },
  "packageManager": "npm@10.8.2",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
